# Allow post-install build scripts for packages that require native compilation.
# These are needed for the Docker deployment build to succeed.
# See: https://pnpm.io/npmrc#approve-builds
approve-builds=true
