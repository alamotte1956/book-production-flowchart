/**
 * typesettingStyles.ts — server entry point
 * All types and data are defined in shared/bibleSpecs.ts and re-exported here
 * so server code can continue to import from this path without changes.
 */
export * from "../shared/bibleSpecs";
